{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/"]
    },
    "build:lib": {
      "outputs": ["dist/ngx-file-uploader"]
    },
    "lint": {},
    "test": {}
  }
}
